# ========================================
# JARVIS - Configuration Environnement
# ========================================
# Généré automatiquement - Instance #27
# Date: 2025-08-20

# =====================================
# SECRETS DE SÉCURITÉ
# =====================================
POSTGRES_PASSWORD=d4b4f6f730802dd0e11d8cc10b54b63edd9670f9b4157a1c58cfdc612c11c3ca
TIMESCALE_PASSWORD=cfe0627da2dc2d1f865d9f78bdf84a67
JARVIS_SECRET_KEY=4f5c6bd7334b2057a5fdec2d0b44afefb942e17b7ca916a00a5bd68b08fc1332

# API Keys externes (à configurer par l'utilisateur)
WEATHER_API_KEY=
HOME_ASSISTANT_TOKEN=
OLLAMA_API_KEY=

# =====================================
# CONFIGURATION RÉSEAU DOCKER
# =====================================
DOCKER_SUBNET=172.20.0.0/16
DOCKER_GATEWAY=172.20.0.1

# =====================================
# ADRESSES IP DES SERVICES
# =====================================
STT_API_IP=172.20.0.10
TTS_API_IP=172.20.0.20
OLLAMA_IP=172.20.0.30
BACKEND_IP=172.20.0.40
INTERFACE_IP=172.20.0.50
POSTGRES_IP=172.20.0.100
REDIS_IP=172.20.0.110
QDRANT_IP=172.20.0.120
TIMESCALE_IP=172.20.0.130

# =====================================
# PORTS DES SERVICES
# =====================================
STT_API_PORT=8003
TTS_API_PORT=8002
BACKEND_PORT=8000
INTERFACE_PORT=3000
WEBSOCKET_PORT=8001
POSTGRES_PORT=5432
REDIS_PORT=6379
OLLAMA_PORT=11434
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334

# =====================================
# CONFIGURATION BASE DE DONNÉES
# =====================================
POSTGRES_DB=jarvis_db
POSTGRES_USER=jarvis
TIMESCALE_DB=jarvis_timeseries
TIMESCALE_USER=jarvis

# =====================================
# URLS DES SERVICES
# =====================================
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_IP}:${POSTGRES_PORT}/${POSTGRES_DB}
REDIS_URL=redis://${REDIS_IP}:${REDIS_PORT}
OLLAMA_BASE_URL=http://${OLLAMA_IP}:${OLLAMA_PORT}
QDRANT_URL=http://${QDRANT_IP}:${QDRANT_HTTP_PORT}
TIMESCALE_URL=postgresql://${TIMESCALE_USER}:${TIMESCALE_PASSWORD}@${TIMESCALE_IP}:${POSTGRES_PORT}/${TIMESCALE_DB}

# URLs API internes
STT_API_URL=http://${STT_API_IP}:${STT_API_PORT}
TTS_API_URL=http://${TTS_API_IP}:${TTS_API_PORT}
BACKEND_API_URL=http://${BACKEND_IP}:${BACKEND_PORT}
INTERFACE_URL=http://${INTERFACE_IP}:${INTERFACE_PORT}
WEBSOCKET_URL=ws://${INTERFACE_IP}:${WEBSOCKET_PORT}/ws

# =====================================
# CONFIGURATION FRONTEND
# =====================================
REACT_APP_API_URL=http://localhost:${BACKEND_PORT}
REACT_APP_WS_URL=ws://localhost:${WEBSOCKET_PORT}

# =====================================
# SERVICES EXTERNES
# =====================================
# Plus de valeurs hardcodées pour localisation
DEFAULT_CITY=Paris
DEFAULT_COUNTRY=FR
DEFAULT_TIMEZONE=Europe/Paris
DEFAULT_LANGUAGE=fr

# Home Assistant (configurable)
HOME_ASSISTANT_URL=http://localhost:8123

# MQTT
MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=

# =====================================
# CONFIGURATION JARVIS
# =====================================
# Memory settings
MEMORY_UPDATE_INTERVAL=604800
MEMORY_RETENTION_DAYS=365
BRAIN_MEMORY_ENABLED=true
EMOTIONAL_ANALYSIS_ENABLED=true
AUTO_CONSOLIDATION_ENABLED=true

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Service Names
SERVICE_NAME_STT=stt-api
SERVICE_NAME_TTS=tts-api
SERVICE_NAME_BACKEND=backend
SERVICE_NAME_INTERFACE=interface

# =====================================
# CHEMINS ET VOLUMES
# =====================================
STT_MODEL_PATH=/app/models/stt
TTS_MODEL_PATH=/app/models/tts
LOGS_PATH=/app/logs
DATA_PATH=/app/data

# =====================================
# CONFIGURATION DE DÉBOGAGE
# =====================================
DEBUG=false
LOG_LEVEL=INFO
ENVIRONMENT=production
# Browserbase MCP Server
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
GEMINI_API_KEY=
