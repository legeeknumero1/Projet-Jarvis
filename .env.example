# =============================================================================
# Configuration Jarvis - Environnement de Développement
# =============================================================================

# -----------------------------------------------------------------------------
# Backend (FastAPI)
# -----------------------------------------------------------------------------
JARVIS_API_KEY=dev-local-key
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:8001"]
LOG_LEVEL=INFO
LOG_FILE=logs/jarvis.log

# Base de données
DATABASE_URL=postgresql://jarvis:jarvis_password@localhost:5432/jarvis_db

# Services externes
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:1b

# Home Assistant (optionnel)
HA_URL=http://localhost:8123
HA_TOKEN=your_ha_token_here

# Weather API (optionnel)
WEATHER_API_KEY=your_weather_api_key

# -----------------------------------------------------------------------------
# Frontend (React)
# -----------------------------------------------------------------------------
# WebSocket (dev local uniquement - pas en prod)
VITE_WS_BASE=ws://localhost:8000
VITE_WS_SECURE=false
VITE_API_KEY=
VITE_USER_ID=enzo

# API REST (fallback si needed)
VITE_API_BASE=http://localhost:8000

# -----------------------------------------------------------------------------
# Docker & Services
# -----------------------------------------------------------------------------
# Ports
BACKEND_PORT=8000
FRONTEND_PORT=3000
POSTGRES_PORT=5432
REDIS_PORT=6379
OLLAMA_PORT=11434

# PostgreSQL
POSTGRES_USER=jarvis
POSTGRES_PASSWORD=jarvis_password
POSTGRES_DB=jarvis_db

# Redis
REDIS_PASSWORD=redis_password

# -----------------------------------------------------------------------------
# Sécurité & Production
# -----------------------------------------------------------------------------
# ⚠️ IMPORTANT: Ne JAMAIS embarquer VITE_API_KEY en production
# En production, utiliser /ws derrière reverse proxy TLS
# Restreindre exposition ports microservices aux réseaux Docker privés